*.iml
.gradle
/local.properties
/.idea/
.DS_Store
/build
/captures
.externalNativeBuild
.cxx
local.properties
# Kotlin Gradle plugin's build-metadata cache (KMP module builds create this at repo root).
/.kotlin

# AOSP PdfRenderer reference sources — kept locally for study/comparison, not ours to
# redistribute as part of this repo's history. See reference/aosp-pdfrenderer/README.md.
/reference/

# Working notes for continuing this project across sessions — not meant for library consumers.
/CLAUDE.md
/DEVLOG.md

# Internal dev convenience script, not meant for library consumers.
/sample/run-ios-simulator.sh
__pycache__/
*.pyc
